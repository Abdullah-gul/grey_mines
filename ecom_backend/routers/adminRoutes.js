// const express = require("express");
// const router = express.Router();
// const authController = require("../controllers/authController");
// const { protect, admin } = require("../middlewares/authMiddleware");

// // Admin route to get all users for search
// router.get("/user",  authController.login);
// // // Admin route to cre delete a user
// // router.delete("/users/:id", protect, admin, adminController.deleteUser);

// // // Admin route to edit a specific user
// // router.put("/users/:id", protect, admin, adminController.updateUser);

// // // Admin route to get a specific user's timer sessions
// // router.get("/users/:userId/sessions", protect, adminController.getUserSessions);

// // // Admin route to create an announcement
// // router.post("/announcements", adminController.createAnnouncement);

// // // Admin route to get all announcements
// // router.get("/announcements", adminController.getAnnouncements);

// // // Admin route to edit an announcement
// // router.put("/announcements/:id", adminController.editAnnouncement);

// // // Admin route to delete an announcement
// // router.delete("/announcements/:id", adminController.deleteAnnouncement);

// // // sdfads

// // // Admin route to create an quote
// // router.post("/quotes", protect, admin, adminController.createQuote);

// // // Admin route to get all quotes
// // router.get("/quotes", protect, adminController.getQuotes);

// // // Admin route to edit an quote
// // router.put("/quotes/:id", protect, admin, adminController.editQuote);

// // // Admin route to delete an quote
// // router.delete("/quotes/:id", protect, admin, adminController.deleteQuote);

// // // Admin route to set current quote
// // router.put(
// //   "/quotes/:quoteId/mark-as-current",
// //   protect,
// //   admin,
// //   adminController.markAsCurrent
// // );

// module.exports = router;
